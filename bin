#!/bin/bash
set -euo pipefail;

name="${1:-pup}";

mkdir "${name}";
cd "${name}";

git init .;
git remote add template https://github.com/otter-academy/template.git;
git fetch template master;
git checkout -b master template/master;

source ./yarn run create;
