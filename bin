#!/bin/bash
set -euo pipefail;

name="${1:-pup}";

mkdir "${name}";
cd "${name}";

git init .;
git remote add upstream https://github.com/otter-academy/template.git;
git fetch upstream master;
git checkout -b master upstream/master;

source ./yarn create;
